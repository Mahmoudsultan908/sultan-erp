-- ════════════════════════════════════════════════════════════
-- مندوب أساسي للعميل — بيتسحب تلقائي كمندوب الفاتورة عند اختيار
-- العميل في فاتورة مبيعات جديدة (لو مفيش مندوب متختار فعلاً).
-- ════════════════════════════════════════════════════════════

ALTER TABLE public.customers
    ADD COLUMN IF NOT EXISTS default_rep_id uuid REFERENCES public.sales_reps(id);
