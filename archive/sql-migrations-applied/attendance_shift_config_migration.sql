-- ════════════════════════════════════════════════════════════
-- نظام "بصمة" للحضور: بدل ما المشرف يختار الحالة يدوي، بيسجل وقت
-- حقيقي بضغطة واحدة والحالة (حاضر/متأخر) بتتحسب من مقارنته بموعد
-- الدوام + فترة السماح. المواعيد دي ممكن تتخصص لكل موظف (اختياري)،
-- ولو فاضية بيستخدم الإعداد العام المخزّن في app_settings تحت مفتاح
-- attendance_shift_default.
-- ════════════════════════════════════════════════════════════

ALTER TABLE employees ADD COLUMN IF NOT EXISTS shift_start_time time;
ALTER TABLE employees ADD COLUMN IF NOT EXISTS grace_minutes integer;
