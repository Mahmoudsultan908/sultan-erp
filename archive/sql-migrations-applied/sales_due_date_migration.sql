-- ════════════════════════════════════════════════════════════
-- إضافة تاريخ استحقاق للفواتير الآجلة — sales_due_date_migration.sql
--
-- عمود بسيط اختياري (nullable)، مفيش أي تريجر أو حساب مالي مرتبط بيه —
-- بيتحدّث بـ UPDATE منفصل من sales.js بعد نجاح fn_create_sale، مش جزء
-- من الترانزاكشن المالية نفسها. آمن 100% على البيانات الموجودة.
--
-- الفواتير الآجلة القديمة (قبل هذا التعديل) هتفضل due_date فاضي —
-- مركز قرار العملاء هيقدّره تلقائياً (تاريخ الفاتورة + 15 يوم) وقت
-- الحساب بدل ما يحتاج UPDATE يملأ كل الصفوف القديمة.
-- ════════════════════════════════════════════════════════════

ALTER TABLE sales ADD COLUMN IF NOT EXISTS due_date date;
